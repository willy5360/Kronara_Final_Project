const getState = ({ getStore, getActions, setStore }) => {
	return {
		store: {
			baseURL: "https://3001-salmon-moth-yk5yf7fo.ws-eu20.gitpod.io/"
		},
		actions: {
			register: data => {
				// fetch(getStore().)
			}
		}
	};
};

export default getState;
